## Tetris Game - ส่วนต่อเติมใหม่

### 🎯 ฟีเจอร์ที่เพิ่มเข้ามา

#### 1. **หน้าเมนูเริ่มเกม (Menu Screen)**
   - ปุ่มเริ่มเกม (เริ่มเกม)
   - ปุ่มออกจากเกม (ออกจากเกม)
   - ชื่อเกมและข้อวามต้อนรับ
   - ปุ่มมีการเปลี่ยนสีเมื่อวาง cursor ไว้

#### 2. **เสียงเอฟเฟค (Sound Effects)**
   - **Rotate Sound**: เสียงเมื่อหมุนชิ้น
   - **Drop Sound**: เสียงเมื่อปล่อยชิ้น
   - **Line Clear Sound**: เสียงเมื่อลบแถว
   - **Game Over Sound**: เสียงเมื่อเกมจบ
   - **Menu Select Sound**: เสียงเมื่อกดเลือกเมนู
   - **Generic Beep**: เสียงทั่วไป

   *(เสียงสร้างจากรหัส Python บริสุทธิ์ ไม่ต้องการไฟล์ external)*

#### 3. **หน้าบอกแพ้ (Game Over Screen)**
   - แสดงข้อความ "แพ้แล้ว!" (ภาษาไทย)
   - แสดง "GAME OVER" เป็นภาษาอังกฤษ
   - แสดงคะแนนสุดท้าย
   - แสดงจำนวนแถวที่ลบได้
   - ปุ่ม "เมนู" (กลับไปหน้าเมนู)
   - ปุ่ม "ออก" (ออกจากเกม)

### 📁 ไฟล์ใหม่ที่สร้างขึ้น

1. **src/sound_manager.py**
   - บริหารจัดการเสียง
   - สร้างเสียง beep อย่างง่าย
   - ไม่ต้องใช้ไฟล์เสียง external

2. **src/menu_screen.py**
   - ระบบอินเทอร์เฟส Menu
   - ระบบ Button ที่โต้ตอบได้
   - Game Over Screen
   - จัดการการคลิกเมาส์

### 📝 ไฟล์ที่ได้รับการแก้ไข

1. **src/config.py**
   - เพิ่มค่า `GAME_MENU` state
   - เพิ่มการตั้งค่าปุ่ม (Button configuration)

2. **src/input_handler.py**
   - เพิ่ม `InputAction.CLICK` สำหรับการคลิกเมาส์
   - เพิ่มการติดตามตำแหน่งเมาส์
   - เพิ่ม method `get_mouse_pos()` และ `is_mouse_clicked()`

3. **src/game.py**
   - เพิ่ม SoundManager integration
   - เพิ่มการจัดการ GAME_MENU state
   - เพิ่มการจัดการ GAME_OVER state
   - เพิ่ม `_start_game()` method
   - เพิ่มเสียงสำหรับการหมุน, ปล่อย, ลบแถว, และแพ้
   - ปรับปรุง render() เพื่อแสดงเมนูและหน้าแพ้

4. **src/renderer.py**
   - นำเข้า MenuScreen และ GameOverScreen
   - เพิ่ม method `render_menu()`, `update_menu()`, `handle_menu_click()`
   - เพิ่ม method `render_game_over()`, `update_game_over()`, `handle_game_over_click()`

### 🎮 การใช้งาน

1. **เมืองเริ่มต้น**:
   - เลือก "เริ่มเกม" เพื่อเริ่มการเล่น
   - เลือก "ออกจากเกม" เพื่อออก

2. **ระหว่างเล่น**:
   - 화살표: ย้ายชิ้นซ้าย/ขวา
   - Z: หมุนชิ้น
   - Space: ปล่อยชิ้นทันที
   - P: หยุดชั่วคราว

3. **เมื่อแพ้**:
   - เลือก "เมนู" เพื่อกลับไปเมนูใหม่
   - เลือก "ออก" เพื่อออกจากเกม

### 🔊 รายละเอียดเสียง

เสียงทั้งหมดสร้างจากรหัส Python บริสุทธิ์ โดยใช้ Pygame mixer:
- เสียงสัญญาณไซน์ที่อัตราตัวอย่าง 22050 Hz
- รูปแบบ stereo 16-bit PCM
- ความถี่ต่างกันสำหรับเสียงต่างใจ

### ✅ การทดสอบ

เกมสามารถรันได้ด้วยคำสั่ง:
```bash
python main.py
```

ทั้งหมดมีความเข้ากันได้กับ OOP และ SOLID Principles
